--- a/system_MIMXRT1062.c
+++ b/system_MIMXRT1062.c
@@ -128,6 +128,11 @@ void SystemInit (void) {
         SCB_EnableICache();
     }
 #endif
+#if defined(__DCACHE_PRESENT) && __DCACHE_PRESENT
+    if (SCB_CCR_DC_Msk != (SCB_CCR_DC_Msk & SCB->CCR)) {
+        SCB_EnableDCache();
+    }
+#endif
 
   SystemInitHook();
 }
